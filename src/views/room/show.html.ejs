<body>
  <section class="cuerpo">
		<h1><%= sala.type%></h1>

		<h2>Capacidad para <%= sala.capacity %> personas</h2>

		<div class=events">
				<h2><%= events.length %> clases disponibles</h2>
				<% if (events.length) { %>
						<ul>
							<% events.forEach((event) => { %>
								<h4> <%= event.name %></h4>
									<p> <%= event.description %></p>
									<p> Días: <%= event.days %></p>
									<p> De <%= event.startsAt %> a <%= event.endsAt %> hrs</p>
							<% }); %>
						</ul>
				<% } %>
		</div>
  </section>
</body>


