<body>
  <section class="cuerpo">
    <h4><%= machine.name%></h4>
      <p><%= machine.description%></p>
        <% if (machine.image){ %>
          <img src="<%= cloudinary.url(machine.image) %>" />
        <% } %>
  </section>
</body>